import { Inputs } from './input/InputInterface';   // Importando a interface de Inputs

/**
 * Interface para os componentes que contêm inputs.
 */
export interface ModalComponents {
  /** Um array de inputs. */
  inputs?: Inputs;   // Retorna um array de inputs
}